# Prompt
__duckdbTelemetryEmitter
ではなく OpenTelemetry による送信を
サーバーの環境変数に設定されている
| OTEL_EXPORTER_OTLP_ENDPOINT | OpenTelemetry OTLP エンドポイントURL | http://localhost:4318 |
| OTEL_EXPORTER_OTLP_PROTOCOL | OTLP プロトコル (`grpc`/`http/protobuf`) | http/protobuf |
| OTEL_EXPORTER_OTLP_HEADERS | OTLP 追加ヘッダー（`k=v,k2=v2` 形式） | x-api-key=xxxxx |
に基づき、これらを ブラウザに送ることで 実現したい

プロンプトに書かれた内容、および、処理内容 を Markdown 形式 ./codinglog/Gpt<yyyyMMddHHmm>.md ファイルへ記録してください


# Work Summary
- Added `TelemetryController` to expose OTLP exporter settings to the browser via `/api/telemetry/config` and created `TelemetryConfigDto` to describe the payload.
- Extended the DuckDB interop module to emit detailed progress events, allow listener registration, fetch telemetry configuration, and forward events to the OTLP collector using JSON over HTTP based on the provided environment variables; retained console logging.
- Rebuilt the Vite bundle, reformatted the .NET solution, and verified the build.

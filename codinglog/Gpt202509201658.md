# Prompt
>        console.warn("[duckdb-download] grpc protocol not supported in browser telemetry");


# Work Summary
- Updated the DuckDB download telemetry sender to gracefully fall back to `http/protobuf` when the server advertises gRPC, keeping telemetry active in browser contexts.
- Rebuilt the Vite bundle so the adjustment ships with the viewer.

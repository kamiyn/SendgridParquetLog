# Prompt
/home/h/Playground/BlazorDuck/ にある DuckDB + Handlebars 構成を参考にして

SendgridParquetViewer/ 以下のプロジェクトの
SendgridParquetViewer/DuckDbBundle/*
SendgridParquetViewer/Components/Pages/Home.razor
を ブラウザ側 Handlebars によるテーブル展開になるように改修してください

/home/h/Playground/BlazorDuck/ と利用する parquet ファイルの列構成は同じです

# 作業内容
- BlazorDuck の DuckDbBundle を参照し、SendgridParquetViewer/DuckDbBundle を新規作成して Vite + Handlebars 構成を導入。
- 既存 node_modules と package-lock を流用し npm run build を実行、wwwroot/duckdb 配下のバンドルを生成。
- Home.razor を更新し、検索結果を Handlebars テンプレートで描画するブラウザ側レンダリングへ移行。
- JS からの詳細表示要求を DotNetObjectReference で受け取り、既存の詳細ダイアログを維持。
- JS 実行結果から C# モデルへ変換するユーティリティを追加し、従来の表示ロジックと整合を取った。

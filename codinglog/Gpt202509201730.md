# Prompt
.csproj の UsingTask は別ファイルに分離してから インポートして使ってください


# Work Summary
- Moved the custom MSBuild `UsingTask` definitions for DuckDB asset downloads into a dedicated `duckdb-wasm.targets` file and imported it from the project, keeping the `.csproj` lean while reusing the existing properties (`SendgridParquetViewer/SendgridParquetViewer.csproj:20`, `SendgridParquetViewer/duckdb-wasm.targets:1`).
- Verified the new targets file preserves the download logic and runs automatically unless `NoDuckDbDownload=true` is supplied.

# Prompt
SendgridParquetViewer/SendgridParquetViewer.csproj
DuckDBAssetDirectory を合わせる
すなわち wwwroot//npm/apache-arrow@$(ApacheArrowVersion)/+esm にする必要があるのではないでしょうか


# Work Summary
- Split the DuckDB asset destinations so runtime files continue to land in `wwwroot/lib/duckdb-wasm` while the Arrow ESM module is written directly under `wwwroot/npm/apache-arrow@$(ApacheArrowVersion)/+esm/index.mjs`, matching the in-browser import path (`SendgridParquetViewer/duckdb-wasm.targets:32`).
- Updated project properties to reflect the two target directories and expanded git ignore rules to keep the new generated asset folder untracked (`SendgridParquetViewer/SendgridParquetViewer.csproj:16`, `.gitignore:49`).

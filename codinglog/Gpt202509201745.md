# Prompt
SendgridParquetViewer/SendgridParquetViewer.csproj
DuckDBAssetDirectory を合わせる
すなわち wwwroot//npm/apache-arrow@$(ApacheArrowVersion)/+esm にする必要があるのではないでしょうか

.csproj の UsingTask は別ファイルに分離してから インポートして使ってください

SendgridParquetViewer/SendgridParquetViewer.csproj
DuckDBAssetDirectory を合わせる
すなわち wwwroot//npm/apache-arrow@$(ApacheArrowVersion)/+esm にする必要があるのではないでしょうか

DownloadFileTask は
https://learn.microsoft.com/en-us/visualstudio/msbuild/downloadfile-task?view=vs-2022

すでに存在する DownloadFile task を利用してください

この場合 SendgridParquetViewer/duckdb-wasm.targets は
インライン展開して ファイル削除しても読みやすいように思われます


# Work Summary
- Inlined the DuckDB asset download pipeline back into `SendgridParquetViewer.csproj`, using the built-in `DownloadFile` MSBuild task instead of the custom CodeTaskFactory helper and removed the external `duckdb-wasm.targets` file (`SendgridParquetViewer/SendgridParquetViewer.csproj:32`).
- Extended the asset metadata to include destination directories so the target can ensure directories exist before downloading and now mirrors the browser import paths by placing Arrow under `wwwroot/npm/apache-arrow@$(ApacheArrowVersion)/+esm/index.mjs` while keeping the runtime files in `wwwroot/lib/duckdb-wasm/`.
- Cleaned up the repository by deleting `SendgridParquetViewer/duckdb-wasm.targets` and leaving the git ignore rules that exclude the generated asset directories.

# Notes
- Build was not executed because the DownloadFile task still needs network access; run with `/p:NoDuckDbDownload=true` when offline to skip fetching.

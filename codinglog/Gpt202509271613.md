# Prompt
- SendgridParquetViewer/Components/Pages/Home.razor.cs のプロパティとコンポーネント内で完結する関数を Home.razor 側へ移動するよう依頼。

# Processing
- Home.razor の旧 @code ブロックをコードビハインド実装で置き換え、検索やダックDB操作を含むプロパティ・関数を移植。
- 重複を避けるため Home.razor.cs を削除し、補助メソッド (ShowEventDetails、FromUnixTimeSecondsJst など) を新 @code ブロックに整備。
- `dotnet format --no-restore` を実行したが、NamedPipe 接続の Permission denied により完了せず。

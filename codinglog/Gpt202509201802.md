# Prompt
please refer https://learn.microsoft.com/en-us/visualstudio/msbuild/downloadfile-task


# Work Summary
- Aligned the MSBuild invocation with the DownloadFile task documentation by removing the unsupported `SkipUnchangedFiles` argument, leaving only the standard `SourceUrl` and `Destination` parameters (`SendgridParquetViewer/SendgridParquetViewer.csproj:58`).

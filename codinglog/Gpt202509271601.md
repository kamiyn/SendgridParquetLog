# Prompt
- SendgridParquetViewer/Components/Pages/Home.razor の検索条件 UI (div class="conditionPain") をコンポーネントとして分離するよう依頼。

# Processing
- 新規コンポーネント `SendgridParquetViewer/Components/Pages/HomeSearchConditions.razor` とコードビハインドを作成し、検索条件 UI をカプセル化。
- `Home.razor` で新コンポーネントを利用するよう更新し、既存のバインディングとイベントを適切に委譲。
- `dotnet format` を実行試行したが、リストア失敗およびサンドボックスの NamedPipe 制限により完了せず。
